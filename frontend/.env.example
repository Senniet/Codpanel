VITE_API_BASE=/api
# Vite dev server proxy target for backend (used by vite.config.ts)
# Set this to your FastAPI dev server if it's not http://localhost:8000
VITE_BACKEND=http://localhost:8000

# App metadata
VITE_APP_NAME=CodPanel
VITE_APP_VERSION=0.1.0
